import {format} from './format.js'
import {p, h, c, A} from './math';


console.log(format('pow', p));
console.log(format('hypotenuse', JSON.stringify(h)));
console.log(format('circleArea', c));
console.log(format('Math.PI', A));

